[defaults]
host_key_checking = False
inventory = inventory.ini
private_key_file = ~/.ssh/hcloud
remote_user = telemaco
timeout = 30
retry_files_enabled = False

[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
pipelining = True
